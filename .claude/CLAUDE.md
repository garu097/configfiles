  # My Preferences

  ## Communication Style
  - Keep explanations concise; skip preamble
  - Show the diff before applying any multi-file change
  - When suggesting code, prefer the language's idiomatic patterns over clever one-liners

  ## Tooling
  - I use zsh on macOS
  - I prefer `uv` over `pip` for Python projects

  ## Habits
  - I always want to see what command Claude is about to run before it runs in production

  ## Sub-agent usage
  - Do not use “it’s faster to do it yourself” as a reason to skip — agents use cheap modal(ex: commit-writer run on Haiku), so the cost is negligible.